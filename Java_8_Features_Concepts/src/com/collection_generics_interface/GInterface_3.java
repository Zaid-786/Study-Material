package com.collection_generics_interface;

//this is generic interface with  type parameter like(E,k,T and etcs)

public interface GInterface_3<E> {

	void setValue(E e);   // method declaration 
	E getValue();
	
}
